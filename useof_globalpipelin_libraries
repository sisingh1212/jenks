@Library("shared-library") _
pipeline {
    
    agent any
    stages {
        stage('build') {
            
            steps {
                
                 helloWorld()
            }
        }
    }
}
